name: Build

permissions:
  contents: write

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up MSVC environment
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libx11-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxi-dev libgl1-mesa-dev libglu1-mesa-dev \
            libfreetype6-dev libfontconfig1-dev ninja-build \
            libpipewire-0.3-dev

      - name: Cache build dependencies
        uses: actions/cache@v5
        with:
          path: |
            build/_deps
            build/CMakeCache.txt
            build/CMakeFiles
          key: deps-${{ runner.os }}-ninja-${{ hashFiles('cmake/**/*.cmake', 'examples/**/CMakeLists.txt', 'CMakeLists.txt') }}
          restore-keys: |
            deps-${{ runner.os }}-ninja-

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DSINGULARITY_BUILD_EXAMPLES=ON

      - name: Build ExampleEffect VST3
        run: cmake --build build --config Release --target ExampleEffect_VST3 --parallel

      - name: Build ExampleEffect Standalone APP
        run: cmake --build build --config Release --target ExampleEffect_APP --parallel

      - name: Build ExampleInstrument VST3
        run: cmake --build build --config Release --target ExampleInstrument_VST3 --parallel

      - name: Build ExampleInstrument Standalone APP
        run: cmake --build build --config Release --target ExampleInstrument_APP --parallel

      - name: Upload ExampleEffect VST3 artifact
        uses: actions/upload-artifact@v7
        with:
          name: ExampleEffect-${{ runner.os }}
          path: build/VST3/**/ExampleEffect.vst3
          if-no-files-found: warn

      - name: Upload ExampleInstrument VST3 artifact
        uses: actions/upload-artifact@v7
        with:
          name: ExampleInstrument-${{ runner.os }}
          path: build/VST3/**/ExampleInstrument.vst3
          if-no-files-found: warn

  release:
    name: Attach artifacts to release
    needs: build
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Zip artifacts
        run: |
          cd artifacts
          for dir in */; do
            zip -r "${dir%/}.zip" "$dir"
          done

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.zip

