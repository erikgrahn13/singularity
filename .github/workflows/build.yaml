name: Build

permissions:
  contents: write

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            cmake_flags: -G Ninja -DCMAKE_BUILD_TYPE=Release -DSINGULARITY_BUILD_EXAMPLES=ON
            cache_key: ninja
            artifact_path: build/VST3/**/ExamplePlugin.vst3
            artifact_name: ExamplePlugin-macOS
          - os: windows-latest
            cmake_flags: -G Ninja -DCMAKE_BUILD_TYPE=Release -DSINGULARITY_BUILD_EXAMPLES=ON
            cache_key: ninja
            artifact_path: build/VST3/**/ExamplePlugin.vst3
            artifact_name: ExamplePlugin-Windows
          - os: ubuntu-latest
            cmake_flags: -G Ninja -DCMAKE_BUILD_TYPE=Release -DSINGULARITY_BUILD_EXAMPLES=ON
            cache_key: ninja
            artifact_path: build/VST3/**/ExamplePlugin.vst3
            artifact_name: ExamplePlugin-Linux

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libx11-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxi-dev libgl1-mesa-dev libglu1-mesa-dev \
            libfreetype6-dev libfontconfig1-dev ninja-build

      - name: Cache build dependencies
        uses: actions/cache@v5
        with:
          path: |
            build/_deps
            build/CMakeCache.txt
            build/CMakeFiles
          key: deps-${{ runner.os }}-${{ matrix.cache_key }}-${{ hashFiles('cmake/**/*.cmake', 'Example/CMakeLists.txt', 'CMakeLists.txt') }}
          restore-keys: |
            deps-${{ runner.os }}-${{ matrix.cache_key }}-

      - name: Configure
        if: runner.os != 'Windows'
        run: cmake -S . -B build ${{ matrix.cmake_flags }}

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cmake -S . -B build ${{ matrix.cmake_flags }}

      - name: Build VST3
        if: runner.os != 'Windows'
        run: cmake --build build --config Release --target ExamplePlugin_VST3 --parallel

      - name: Build VST3 (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cmake --build build --config Release --target ExamplePlugin_VST3 --parallel

      - name: Build Standalone APP
        if: runner.os != 'Windows'
        run: cmake --build build --config Release --target ExamplePlugin_APP --parallel

      - name: Build Standalone APP (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cmake --build build --config Release --target ExamplePlugin_APP --parallel

      - name: Upload VST3 artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: warn

  release:
    name: Attach artifacts to release
    needs: build
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Zip artifacts
        run: |
          cd artifacts
          for dir in */; do
            zip -r "${dir%/}.zip" "$dir"
          done

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.zip

